# SonarCloud Project Configuration
# Automatically configured via GitHub Actions workflow

sonar.projectKey=MaterialHub-Organization_materials-hub
sonar.organization=materialhub-organization

# Project metadata
sonar.projectName=Materials Hub
sonar.projectVersion=1.0

# Source code
sonar.sources=app,core,rosemary
sonar.tests=app/modules
sonar.test.inclusions=**/*test*.py

# Python version
sonar.python.version=3.12

# Coverage
sonar.python.coverage.reportPaths=coverage.xml

# Exclusions
sonar.exclusions=**/migrations/**,**/tests/**,**/static/**,**/templates/**,**/__pycache__/**,**/venv/**,**/htmlcov/**,**/*.egg-info/**,**/node_modules/**
sonar.coverage.exclusions=**/tests/**,**/test_*.py,**/conftest.py,**/migrations/**,**/seeders.py,rosemary/commands/**

# Encoding
sonar.sourceEncoding=UTF-8

# Quality Gate (informational - adjust these later)
sonar.qualitygate.wait=false
